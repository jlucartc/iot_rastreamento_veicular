import '../scss/toast.scss';
export default class Toast {
    element: HTMLElement;
    plugins: ToastPlugin[];
    /**
     * Create a new toast
     */
    constructor(data: any, plugins?: string[], template?: ((data: any) => string) | undefined);
    /**
     * Instantiate the template
     */
    template({ className, title, content }: any): string;
    /**
     * Hide the toast
     */
    hide(): void;
    /**
     * Destroy the toast
     */
    destroy(): void;
    /**
     * Activate the plugins
     */
    enablePlugins(): void;
    /**
     * Disable the plugins
     */
    disablePlugins(): void;
    /**
     * Collect and instantiate the plugins available for the toast
     */
    private collectPlugins;
}
